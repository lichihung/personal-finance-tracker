Find the most recent git tag, then list all commits between that tag and HEAD using `git log <tag>..HEAD --oneline`. Translate those commits into clean, user-facing release notes in English — not developer jargon. Group them into categories if helpful (e.g. improvements, fixes). Keep each point short and clear, written for end users not developers. If there are no commits since the last tag, say so. Finally, show the last tag name so the user knows what version they're comparing from.
